# news_digest — Python 3.11+
# Install with: pip install -r requirements.txt

# Core LLM / embedding
openai>=1.30.0               # async AsyncOpenAI client
chromadb>=0.5.0              # vector store

# HTTP (async, replaces synchronous newspaper3k)
httpx>=0.27.0                # async HTTP with redirect follow + timeout

# RSS
feedparser>=6.0.11

# Config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0     # BaseSettings with .env support

# HTML templating (safe escaping)
Jinja2>=3.1.4

# Dev / optional
python-dotenv>=1.0.0         # auto-loaded by pydantic-settings when present
